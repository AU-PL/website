(* 1. Define a variable using a let-expression called 'favMove' and set it to
      your favorite movie as a string (string's in OCaml are written using
      double-quotes.) *)


(* 2. Define a function, called maxi, that takes in two integer parameters and
       returns the max between them.  *)


